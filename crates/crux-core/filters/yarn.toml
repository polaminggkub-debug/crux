command = "yarn"
description = "Filter yarn output: skip resolution/fetch progress"
priority = 0
strip_ansi = true

skip = [
    "^\\[\\d+/\\d+\\] (Resolving|Fetching|Linking)",
    "^info ",
    "^warning .* No license field",
]

collapse_blank_lines = true
trim_trailing_whitespace = true
