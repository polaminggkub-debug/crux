command = "ansible"
description = "Filter ansible output: keep changed/failed/fatal, skip ok/skipping tasks"
priority = 0
strip_ansi = true

skip = [
    "^ok: \\[",
    "^skipping: \\[",
    "^\\s*$",
]

collapse_blank_lines = true
trim_trailing_whitespace = true
